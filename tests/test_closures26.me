meme central:memescript/0.0.3/compiler
requires test

// -- module functions --

main: fun() {
  var z = fun() {  };
  test.assertEqual(z(), null, "testing empty closure");
}

// -- module classes --
